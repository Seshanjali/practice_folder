#ifndef _SWAP_BITS_WITHIN__H__
#define _SWAP_BITS_WITHIN__H__

#include <stdio.h>
#define SIZE 100

int integer_validation(char []);
int integer_conversion(char []);
int bit_conversion(int);
unsigned int copy_bits(unsigned int, unsigned int, unsigned int, unsigned int, unsigned int);

#endif
