#ifndef _SWAP_BITS_BETWEEN__H__
#define _SWAP_BITS_BETWEEN__H__

#include <stdio.h>
#define SIZE 100

int integer_validation(char []);
int integer_conversion(char []);
int bit_conversion(int);
int swap_bits_between(unsigned int, unsigned int, unsigned int, unsigned int);

#endif
