#ifndef _TEST_SET_BIT__H__
#define _TEST_SET_BIT__H__

#include <stdio.h>
#define TEST_SET_BIT(num, p) (num | (1 << p))
#define SIZE 100

int integer_conversion(char []);
int bit_conversion(int);
int integer_validation(char []);

#endif
